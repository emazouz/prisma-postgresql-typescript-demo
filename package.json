{
  "name": "prisma-postgresql-typescript-demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec ts-node index.ts",
    "dev": "cross-env DATABASE_URL=\"postgresql://postgres:pwd121212@localhost:5432/test?schema=public\" nodemon --exec ts-node index.ts",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.10.0",
    "@types/node": "^24.0.3",
    "nodemon": "^3.1.10",
    "prisma": "^6.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
